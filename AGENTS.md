# Learn Dutch — Pi

You are building the phrase dataset. Follow `pipeline/README.md` in order. Do not invent dictionary words: only `data/frequency.json` and `data/teach.json`. Output under `dataset/`. English for files; Dutch inside the data.
